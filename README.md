# Daily Habits

A mobile-first habit tracker for daily checkmarks, day navigation, and simple persistent storage.

## Run

```bash
python3 server.py
```

Then open:

```text
http://127.0.0.1:4174/
```

Habit data is stored locally in `data/habit-data.json`, which is intentionally ignored by Git.
